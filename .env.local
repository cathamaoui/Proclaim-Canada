# Database configuration
# Neon PostgreSQL (free): https://neon.tech
# Create account → Create Project → Copy connection string below
DATABASE_URL="postgresql://user:password@hostname/dbname"

# Authentication
NEXTAUTH_SECRET="test-secret-for-development-only-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Email configuration (Development)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@proclaimcanada.com"
